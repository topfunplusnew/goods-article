# Test Setup

This project uses:

- `tsx --test` for module-level TypeScript tests
- Playwright for end-to-end route checks

Keep unit-style tests under `tests/` and browser checks under `tests/e2e/`.
